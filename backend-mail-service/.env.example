# Environment
NODE_ENV=development

# Server
PORT=3052

# SMTP Configuration
# Choisissez l'une des options ci-dessous et décommentez-la

# Option 1: Gmail (Production recommandée)
# 1. Activez la validation en 2 étapes sur votre compte Gmail
# 2. Générez un "App Password" : https://myaccount.google.com/apppasswords
# 3. Utilisez ce mot de passe d'application ci-dessous
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=votre-email@gmail.com
# SMTP_PASS=votre-app-password-16-caracteres
# SMTP_FROM=votre-email@gmail.com

# Option 2: Mailtrap.io (Développement recommandé)
# 1. Créez un compte sur https://mailtrap.io (gratuit)
# 2. Copiez vos identifiants SMTP depuis votre inbox
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_SECURE=false
# SMTP_USER=votre-username-mailtrap
# SMTP_PASS=votre-password-mailtrap
# SMTP_FROM=noreply@chift.sn

# Option 3: Serveur SMTP personnalisé
SMTP_HOST=mail.simplonsolution.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=simplon@simplonsolution.com
SMTP_PASS=Bonjour2024@
SMTP_FROM=simplon@simplonsolution.com

# Sender Info (affiché dans les emails)
EMAIL_FROM=noreply@chift.sn
EMAIL_FROM_NAME=Chift Platform

# CORS (frontend URLs autorisées)
CORS_ORIGIN=http://localhost:3046,http://localhost:3000

# Rate Limiting (protection anti-spam)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
